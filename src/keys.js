// muse/keys — the localStorage keys, defined once. Every module that reads or
// writes persisted data imports from here so a rename can't drift.

export const STATE_KEY = 'muse:state';
export const CUSTOM_FONTS_KEY = 'muse:custom-fonts';
export const CUSTOM_THEMES_KEY = 'muse:custom-themes';
export const FOUND_FONTS_KEY = 'muse:found-fonts';
